import React from 'react';
import useTitle from '../../Hooks/useTitle';

const MyReviews = () => {
    useTitle('My reviews')
    return (
        <div>
            <h1>reviews</h1>
        </div>
    );
};

export default MyReviews;